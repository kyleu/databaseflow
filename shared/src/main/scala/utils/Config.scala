package utils

object Config {
  val projectId = "databaseflow"
  val projectName = "Database Flow"
  val projectSlug = "database-flow"
  val projectUrl = "https://databaseflow.com"
  val version = "0.1"
  val pageSize = 100
}
